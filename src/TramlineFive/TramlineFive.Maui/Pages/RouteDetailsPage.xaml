<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TramlineFive.Maui.Pages.RouteDetailsPage"
             xmlns:vm="clr-namespace:TramlineFive.Common.ViewModels;assembly=TramlineFive.Common"
             xmlns:cv="clr-namespace:TramlineFive.Converters"
             BindingContext="{Binding RouteDetailViewModel, Source={StaticResource Locator}}"
             Title="RouteDetails">

    <ContentPage.Resources>
        <ResourceDictionary>
            <cv:TransportTypeToImageConverter x:Key="TransportToImage" />
            <cv:TransportTypeToColorConverter x:Key="TransportToColor" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ScrollView Background="White">
        <VerticalStackLayout>
            <HorizontalStackLayout Background="White" Padding="10">
                <HorizontalStackLayout Background="{Binding Arrival.VehicleType, Converter={StaticResource TransportToColor}}" Padding="10" HeightRequest="50">
                    <Label Text="{Binding Arrival.LineName}" FontSize="24" FontAttributes="Bold" VerticalTextAlignment="Center" HorizontalOptions="Center" VerticalOptions="Center" TextColor="White" />
                </HorizontalStackLayout>
                <Label Text="{Binding StopName}" FontSize="24" TextColor="Black" VerticalTextAlignment="Center" Margin="15,0,0,0" />
            </HorizontalStackLayout>

            <ActivityIndicator IsRunning="{Binding IsLoading}" 
                                   IsVisible="{Binding IsLoading}" 
                                   HorizontalOptions="Center" 
                                   HeightRequest="40"/>
            <HorizontalStackLayout Spacing="10" Margin="15,10,0,0">

                <Border Padding="10" BackgroundColor="DodgerBlue" StrokeShape="RoundRectangle 12" StrokeThickness="0">
                    <HorizontalStackLayout Spacing="5" VerticalOptions="Center">
                        <Label FontFamily="MaterialIconsOutlinedRegular.otf" TextColor="White" Text="&#xe55b;" FontSize="18" />
                        <!-- map icon -->
                        <Label Text="Маршрут" TextColor="White" VerticalOptions="Center"/>
                    </HorizontalStackLayout>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding ViewRouteCommand}" />
                    </Border.GestureRecognizers>
                </Border>

                <Border Padding="10" BackgroundColor="DodgerBlue"  StrokeShape="RoundRectangle 12" StrokeThickness="0">
                    <HorizontalStackLayout Spacing="5" VerticalOptions="Center">
                        <Label FontFamily="MaterialIconsOutlinedRegular.otf" Text="&#xe530;" TextColor="White" FontSize="18" />
                        <!-- vehicle icon -->
                        <Label Text="Местоположение" TextColor="White" VerticalOptions="Center"/>
                    </HorizontalStackLayout>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding ViewVehicleCommand}" />
                    </Border.GestureRecognizers>
                </Border>
            </HorizontalStackLayout>


            <VerticalStackLayout Margin="5,15,0,0">
                <!-- Realtime Arrivals -->
                <CollectionView ItemsSource="{Binding Arrival.Arrivals}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border StrokeThickness="0" Margin="0,0,0,5" BackgroundColor="White" StrokeShape="RoundRectangle 5">
                                <Grid ColumnDefinitions="10, *">
                                    <BoxView WidthRequest="7" Margin="-3,0,0,0" Color="{Binding BindingContext.Arrival.VehicleType, Converter={StaticResource TransportToColor}, Source={RelativeSource AncestorType={x:Type CollectionView}}}" />
                                    <Grid Grid.Column="1" Margin="5" Padding="0,10,10,10" ColumnDefinitions="Auto, Auto, *, Auto" VerticalOptions="Center">
                                        <Label Grid.Column="1" VerticalTextAlignment="Center" FontFamily="MaterialIconsOutlinedRegular.otf" Text="&#xe51e;" FontSize="25" Margin="0,0,5,0" TextColor="Green" IsVisible="{Binding Realtime}" />
                                        <VerticalStackLayout Grid.Column="2" VerticalOptions="Center">
                                            <HorizontalStackLayout>
                                                <Label FontFamily="MaterialIconsOutlinedRegular.otf" Text="&#xe52e;" FontSize="21" Margin="0,0,5,0" TextColor="Black" />
                                                <Label Text="{Binding Direction}" FontSize="Default" VerticalOptions="Center" LineBreakMode="TailTruncation"/>
                                            </HorizontalStackLayout>
                                        </VerticalStackLayout>
                                        <VerticalStackLayout Grid.Column="3">
                                            <Label Text="{Binding MinutesTillArrival}" FontSize="21" VerticalOptions="Center" HorizontalOptions="Center" TextColor="Green"/>
                                            <Label Text="мин" />
                                        </VerticalStackLayout>
                                    </Grid>
                                    <BoxView HeightRequest="1" Margin="0,0,0,0" VerticalOptions="End" Color="LightGray" Grid.ColumnSpan="2" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- Scheduled Arrivals -->
                <CollectionView ItemsSource="{Binding ScheduledArrivals}" ItemSizingStrategy="MeasureFirstItem">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border StrokeThickness="0" BackgroundColor="White" StrokeShape="RoundRectangle 5">
                                <Grid ColumnDefinitions="10, *">
                                    <BoxView WidthRequest="7" Margin="-3,0,0,0" Color="{Binding BindingContext.Arrival.VehicleType, Converter={StaticResource TransportToColor}, Source={RelativeSource AncestorType={x:Type CollectionView}}}" />
                                    <Grid Grid.Column="1" Margin="5" Padding="0,10,10,10" ColumnDefinitions="Auto, Auto, *, Auto" VerticalOptions="Center">
                                        <Label VerticalOptions="Center" Grid.Column="1" FontFamily="MaterialIconsOutlinedRegular.otf" Text="&#xE8B5;" TextColor="Gray" FontSize="25" Margin="0,0,5,0" />
                                        <VerticalStackLayout Grid.Column="2" VerticalOptions="Center">
                                            <HorizontalStackLayout>
                                                <Label FontFamily="MaterialIconsOutlinedRegular.otf" Text="&#xe52e;" FontSize="21" Margin="0,0,5,0" TextColor="Black" />
                                                <Label Text="{Binding Headsign}"  FontSize="Default" VerticalOptions="Center" LineBreakMode="TailTruncation"/>
                                            </HorizontalStackLayout>
                                            <Label Text="{Binding DepartureTime}" FontSize="14" TextColor="Gray" VerticalOptions="Center" LineBreakMode="TailTruncation"/>
                                        </VerticalStackLayout>
                                        <VerticalStackLayout Grid.Column="3">
                                            <Label Text="{Binding MinutesDisplay}" FontSize="21" VerticalOptions="Center" HorizontalOptions="Center" />
                                            <Label Text="мин" HorizontalOptions="Center" />
                                        </VerticalStackLayout>
                                    </Grid>
                                    <BoxView HeightRequest="1" Margin="0,0,0,0" VerticalOptions="End" Color="LightGray" Grid.ColumnSpan="2" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>