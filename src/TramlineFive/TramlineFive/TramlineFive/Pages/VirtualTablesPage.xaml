<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:cv="clr-namespace:TramlineFive.Converters"
             x:Class="TramlineFive.Pages.VirtualTablesPage"
             Title="Виртуални табла">

    <ContentPage.Resources>
        <ResourceDictionary>
            <cv:InverseBoolConverter x:Key="InverseBool" />
            <cv:ReferenceToBoolConverter x:Key="ReferenceToBool" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid Padding="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="110" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <StackLayout HorizontalOptions="Center">
            <Entry Placeholder="Код на спирка..." Text="{Binding StopCode}" Completed="OnStopCodeCompleted" />
            <Button Text="Проверяване" IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBool}}" Clicked="OnCheckClicked" />
        </StackLayout>

        <Grid Grid.Row="1" Margin="-10,15,-10,0">
            <StackLayout Margin="0,10,0,0" HorizontalOptions="Center">
                <ActivityIndicator IsEnabled="{Binding IsLoading}" IsVisible="{Binding IsLoading}" IsRunning="{Binding IsLoading}" />
                <Label Text="{Binding Message}" IsVisible="{Binding NoTimings}" />
            </StackLayout>
            <StackLayout BackgroundColor="DodgerBlue" Grid.Row="1" Padding="5" IsVisible="{Binding SelectedLine, Converter={StaticResource ReferenceToBool}}">
                <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
                    <Label Text="{Binding StopCode, StringFormat='({0})'}" TextColor="White" FontSize="14" />
                    <Label Text="{Binding CurrentStop.Name}" TextColor="White" FontSize="14" />
                </StackLayout>
                <Label Text="{Binding CurrentStop.Direction}" HorizontalOptions="Center" HorizontalTextAlignment="Center" TextColor="White" FontSize="14" />
                <Label Text="{Binding SelectedLine}" HorizontalOptions="Center" VerticalOptions="Center" TextColor="White" FontSize="14" />
            </StackLayout>
        </Grid>

        <ListView Grid.Row="2" ItemsSource="{Binding Timings}" HorizontalOptions="Center" />

        <Label Text="{Binding Version.VersionNumber, StringFormat='Налична е версия v{0}. Кликнете за сваляне.'}" Grid.Row="3"
               TextColor="DodgerBlue" HorizontalOptions="Center">
            <Label.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnVersionTapped" />
            </Label.GestureRecognizers>
        </Label>
    </Grid>
</ContentPage>
