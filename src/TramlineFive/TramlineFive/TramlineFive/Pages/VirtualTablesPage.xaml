<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:cv="clr-namespace:TramlineFive.Converters"
             x:Class="TramlineFive.Pages.VirtualTablesPage"
             Title="Виртуални табла">

    <ContentPage.Resources>
        <ResourceDictionary>
            <cv:InverseBoolConverter x:Key="InverseBool" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid Padding="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="100" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <StackLayout HorizontalOptions="Center">
            <Entry Placeholder="Код на спирка..." Text="{Binding StopCode}" Completed="OnStopCodeCompleted" />
            <Button Text="Проверяване" IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBool}}" Clicked="OnCheckClicked" />
            <ActivityIndicator IsEnabled="{Binding IsLoading}" IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" />
            <Label Text="{Binding Message}" IsVisible="{Binding NoTimings}" />
        </StackLayout>

        <Grid BackgroundColor="DodgerBlue" Grid.Row="1" Margin="0,15,0,0">
            <Label Text="{Binding SelectedLine}" HorizontalOptions="Center" VerticalOptions="Center" TextColor="White" />
        </Grid>

        <ListView Grid.Row="2" ItemsSource="{Binding Timings}" HorizontalOptions="Center" />

        <Label Text="{Binding Version.VersionNumber, StringFormat='Налична е версия v{0}. Кликнете за сваляне.'}" Grid.Row="3"
               TextColor="DodgerBlue" HorizontalOptions="Center">
            <Label.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnVersionTapped" />
            </Label.GestureRecognizers>
        </Label>
    </Grid>
</ContentPage>
